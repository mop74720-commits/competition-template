# Route Cards

## Candidate / Route: <name>

- deliverable:
- baseline_or_minimal_witness:
- primary_route:
- binding_constraints:
- engineering_risk:
- deciding_probe:
- deciding_evidence_needed:
- rejected_alternative (only if real competition exists):
- refutation_test:
- flip_condition:
- fallback_trigger:
- fallback_action:
- judge_visible_evidence:

---

低不确定性时可以只保留 baseline + primary；不要为了形式制造多路线。
